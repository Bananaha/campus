include ../mixins/main

.module-card

  .actionDetails

    h2.actionDetails-title {{accompagnement.intitule}}
    p.actionDetails-id Action n°{{accompagnement.action}}

    ul.actionDetails-list
      li(ng-repeat="item in accompagnement.list")
        span.actionDetails-label {{item.label}}
        span.actionDetails-value {{item.value}}

    .module-footer
      button(ng-click="modifier()").btn.pull-right Modifier
