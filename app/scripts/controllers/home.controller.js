(function (global, angular) {
    'use strict';

    angular.module('campus.app')
    .controller('homeController',
        function (
            config,
            $scope,
            $http
        ) {

        });

}(window, window.angular));